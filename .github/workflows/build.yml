name: Build

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - "atlas_api.cpp"
      - "atlas_ffi.h"
      - "compile.bat"
      - ".github/workflows/build.yml"
      - "tests/*.py"
      - "tests/*.json"
  pull_request:
    branches: [master]
    paths:
      - "atlas_api.cpp"
      - "atlas_ffi.h"
  release:
    types: [published]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install LLVM/Clang
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y clang libomp-dev

      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: clang++ -fopenmp -O2 -mavx2 -mfma -mf16c -ffast-math -std=c++17 -fPIC -shared -o libatlas.so atlas_api.cpp -lgomp

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $llvmDir = "C:\Program Files\LLVM"
          if (-not (Test-Path $llvmDir)) { throw "LLVM not found at $llvmDir" }
          clang++ -fopenmp -O2 -mavx2 -mfma -mf16c -ffast-math -std=c++17 -shared -o atlas.dll atlas_api.cpp "$llvmDir\lib\libomp.lib"
          if ($LASTEXITCODE -ne 0) { throw "Build failed" }
          Copy-Item "$llvmDir\bin\*.dll" -Destination . -Force -ErrorAction SilentlyContinue
          Write-Output "=== Copied $(@(Get-ChildItem *.dll).Count) DLLs ==="
          echo "$llvmDir\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Install test deps
        run: pip install --quiet pytest numpy safetensors "fastapi>=0.115.0" uvicorn

      - name: Generate test fixtures
        shell: pwsh
        run: python tests/generate_test_fixtures.py tests/

      - name: Test (Linux)
        if: runner.os == 'Linux'
        run: |
          python tests/ci_smoke.py
          python -m pytest tests/test_dll.py -v
          python -m pytest tests/test_fixtures.py -v

      - name: Test (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:KMP_DUPLICATE_LIB_OK="TRUE"
          python tests/ci_smoke.py
          python -m pytest tests/test_dll.py -v
          $env:GENERATE_GOLDEN="1"
          python -m pytest tests/test_fixtures.py::test_golden_regression -v --no-header -q 2>$null
          $env:GENERATE_GOLDEN="0"
          python -m pytest tests/test_fixtures.py -v

  # macOS only on release (free tier runner bottleneck)
  build-macos:
    if: github.event_name == 'release'
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - name: Install LLVM/Clang
        run: brew install llvm libomp
      - name: Build
        run: |
          LLVM_PREFIX=$(brew --prefix llvm)
          $LLVM_PREFIX/bin/clang++ -fopenmp -O2 -mavx2 -mfma -mf16c -ffast-math -std=c++17 -fPIC -shared -o libatlas.so atlas_api.cpp -L$LLVM_PREFIX/lib -lomp
          echo "DYLD_LIBRARY_PATH=$LLVM_PREFIX/lib:${DYLD_LIBRARY_PATH:-}" >> $GITHUB_ENV
      - name: Install test deps
        run: pip install --quiet pytest numpy safetensors "fastapi>=0.115.0" uvicorn
      - name: Generate test fixtures
        run: python tests/generate_test_fixtures.py tests/
      - name: Test
        run: |
          python tests/ci_smoke.py
          python -m pytest tests/test_dll.py -v
          python -m pytest tests/test_fixtures.py -v
      - name: Upload artifact (release only)
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: atlas-macOS
          path: libatlas.so
